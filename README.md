# bvp_lrs

Boundary-value problem (BVP) derived learning-rate schedules (LRS) for deep learning.

`bvp_lrs` is a compact library for generating learning-rate schedules from boundary-value problem (BVP) profiles. Instead of hardcoding a single formula, you can shape the schedule using BVP families and tune parameters to express warmup, plateau or more complex multi-stage behavior in addition to decay behavior.

<p align="center">
<img src="figures/bvp_case1_plt.gif" width="300"
</p>

## Overview

This repository provides implementation of a family of learning-rate schedules built from boundary-value problem (BVP) profiles. The schedule design follows a normalized training horizon `r in [0,1]`, where the profile captures the desired shape of the expected step-size magnitude over time, and a smooth variational window function (a linear window or its regularized version, the raised-cosine window) converts that profile into a practical learning-rate curve.

The code is available in both NumPy and PyTorch, making it suitable for research, prototyping, and optimizer experiments.

- Numpy: `bvp_lrs_np.py`
- PyTorch: `bvp_lrs_torch.py`

## Core Logic

Let `r` denote the normalized iteration index over the training horizon.

A BVP profile `x(r)` is constructed to encode a schedule shape such as:

- warmup,
- plateau,
- multi-stage or asymmetric phases of increase/decrease,

into the annealing operation. The final schedule is then generated by passing the profile into a window function.

**Case 1**:

- p-th root linear window: `case1_lin(x, p) = (1 - x)^(1/p)`
- p-th root raised cosine window: `case2_rcos(x, p) = cx^(1/p)`, where `cx = cos(0.5 * pi * x)^2`

These windows are the smoothest over the normalized domain with respect to the first-order total variational energy functional of the expected step-size magnitude, and provide a template for optimizer step-size control.

**Case 2**:

- p-th root quadratic polynomial window: `case2_lin(x, p) = ((1 + 2 x)(1 - x))^(1/p)`
- p-th root sum of raised cosines window: `case2_rcos(x, p) = ((3 - 2 cx) cx)^(1/p)`, where `cx = cos(0.5 * pi * x)^2`

These windows additionally maximize the total expected step-size magnitude while remaining smooth over the normalized domain with respect to the first-order total variational energy functional of the expected step-size magnitude.

## Repository structure

- `bvp_lrs_np.py` — NumPy implementations of a BVP profile and window functions
- `bvp_lrs_torch.py` — PyTorch equivalents for optimizer-integrated use
- `case1_anim.py` — animation of 2-point BVP schedules
- `case2_anim.py` — animation of 3-point BVP schedules
- `figures/` — generated plots and GIFs

## BVP profile

A 2-point to 6-point profile can be generated using:

- `x = bvp_prof(r, m, e)`

with two interpretable knobs:

- m to specify warmup fraction
- e to specify plateau fraction

This supports a transition between warmup, overshoot, plateau and decay-like behavior to enable multi-stage training behavior.


## Window functions

The library includes primary window constructions based on two cases.

### Case 1: Minimizing a first-order total variational energy functional subject to boundary value constraints.

#### Linear window

```python
from bvp_lrs_np import case1_lin

schedule = case1_lin(profile, p=1)
```

This is the normal shape based on minimizing a first-order total variational energy functional.

#### Raised-cosine window

```python
from bvp_lrs_np import case1_rcos

schedule = case1_rcos(profile, p=1)
```

This shape is derived by applying a Tikohonov regularization to the first-order total variational energy functional.

#### Case 1: Usage example

```python
import numpy as np

from bvp_lrs_np import bvp_prof, case1_lin, case1_rcos

# normalized horizon of learning iterations
r = np.linspace(0.0, 1.0, 1000)

# 10% warmup, 20% constant
bvp_profile = bvp_prof(r, m=0.1, e=0.2)

# Dirichlet-energy minimizing window
schedule = case1_lin(bvp_profile, p=1)

# Regularized Dirichlet-energy minimizing window
reg_schedule = case1_rcos(bvp_profile, p=1)

```

#### Case 1: PyTorch integration

```python

import torch
from bvp_lrs_torch import bvp_prof, case1_lin, case1_rcos

'''RMSProp'''

# get the current normalized time-step
r_t = (step - 1) / (num_iterations - 1)
# note: this locally converts the default one-based index start design used 
# in Pytorch's optimizer class to a zero-based index start

# normalize the stochastic gradient 'grad' using its second moment estimate
smom = 0.999*smom + 0.001*(grad*grad)
grad_smom = smom/(1 - (0.999**step))
grad_smom = torch.sqrt(smom).add(1e-10)

# apply a square-root linear decay schedule with 10% warmup 
# to the maximum step-size 'mu'
mu_t = mu * glin(bvp_prof(r_t, m=0.1), p=2)
lr_t = mu_t / grad_smom

# update the model parameter 'p' using 
# the learning-rate 'lr_t' and 'grad'
p = p - lr_t * grad

```

This RMSProp example above shows how to plug the schedule into a PyTorch Optimizer class.

### Case 2: Maximize the total step-size magnitude functional while Minimizing the total step-size first-order variational energy functional subject to boundary conditions.

<p align="center">
<img src="figures/bvp_case2_plt.gif" width="300"
</p>

#### Quadratic Polynomial window

```python
from bvp_lrs_np import case2_lin

schedule = case2_lin(profile, p=1)
```

This is the normal shape.

#### Sum of Raised-cosines window

```python
from bvp_lrs_np import case2_rcos

schedule = case2_rcos(profile, p=1)
```

This shape is derived by adding a Tikohonov regularization to the energy functional.

#### Case 2: Usage example

```python
import numpy as np

from bvp_lrs_np import bvp_prof, case2_lin, case2_rcos

# normalized horizon of learning iterations
r = np.linspace(0.0, 1.0, 1000)

# 10% warmup, 20% constant
bvp_profile = bvp_prof(r, m=0.1, e=0.2)

# Dirichlet-energy minimizing window
schedule = case2_lin(bvp_profile, p=1)

# Regularized Dirichlet-energy minimizing window
reg_schedule = case2_rcos(bvp_profile, p=1)

```

#### Case 2: PyTorch integration

```python

import torch
from bvp_lrs_torch import bvp_prof, case2_lin, case2_rcos

'''RMSProp'''

# get the current normalized time-step
r_t = (step - 1) / (num_iterations - 1)
# note: this locally converts the default one-based index start design used 
# in Pytorch's optimizer class to a zero-based index start

# normalize the stochastic gradient 'grad' using its second moment estimate
smom = 0.999*smom + 0.001*(grad*grad)
grad_smom = smom/(1 - (0.999**step))
grad_smom = torch.sqrt(smom).add(1e-10)

# apply the quadratic polynomial schedule with 10% warmup 
# to an initial guess of the maximum step-size 'mu'
mu_t = mu * case2_lin(bvp_prof(r_t, m=0.1))
lr_t = mu_t / grad_smom

# update the model parameter 'p' using 
# the learning-rate 'lr_t' and 'grad'
p = p - lr_t * grad

```

## Visualization

The project includes animation scripts to inspect how the BVP families evolve with respect to their parameters:

```bash
python case1_anim.py
python case2_anim.py
```

These scripts help visualize profile shape and overall schedule behavior.

<p align="center">
<img src="figures/bvp_case1_plt.png" width="300"
</p>

<p align="center">
<img src="figures/bvp_case2_plt.png" width="300"
</p>

## Related work and citation

This code is based on a trust-region framework for constructing learning-rate schedules using smooth variational windows. The underlying methodology is described in the related preprint:

<a href="https://somefunagba.github.io/assets/pdf/vantr_lrschedule.pdf" target="_blank">A Variational Trust-Region Framework
for Learning-Rate Annealing</a>

> [!NOTE]
> A useful way to interpret the paper is that popular learning‑rate schedules in deep learning are **boundary-value constrained trust‑region trajectories**, with shapes determined by a **first‑order total variational energy principle**.

From first-principles, one can then derive

- [x] Linear decay
- [x] Square‑root linear decay
- [x] Cosine annealing (second-order raised cosine)
- [x] Cosine annealing (first-order raised cosine)

as solutions to a 2-point BVP (Case 1). Extending this to multi-point BVPs yield

- [x] Warmup‑decay
- [x] Warmup‑stable‑decay

as natural consequences of the same underlying variational framework.

Case 2 then goes beyond deriving existing popular schedules in the literature.

## Notes

This repository is compact and provides an extensible set of schedule-building primitives that can be adapted to optimizer design and experimentation.
Please, cite when used.
